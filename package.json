{
  "name": "es-due-diligence-tracker",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"node api/index.js\" \"cd client && npm start\"",
    "build": "cd client && npm install && npm run build",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.1",
    "bcryptjs": "^2.4.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
